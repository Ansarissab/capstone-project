import React from 'react'

const BucketListCard = () => {
  return (
    <div>BucketListCard is created by post method, which takes a name of the bucketlist
         and a number of activity cards that are added to the bucket list by a user
    </div>
  )
}

export default BucketListCard